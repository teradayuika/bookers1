<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <title>Bookers</title>
  </head>

  <body>
    
    <h1>Bookers</h1>
    
    
    <table>
        <theadr>
        <tr>
        <th>Title</th>
        <th>Boby</th>
        <th colspan="3"></th>
        <tr>
        </theadr>
        
        <tboby>
        <%= @books.each do |book| %>
            <td><%= book.title %></td>
            <td><%= book.body %></td>
            <td><%= link_to 'Show', show_path(book.id) %></td>
            <td><%= link_to 'Edit', edit_path(book.id) %></a></td>
            <td><%= link_to "Destroy", destroy_path(book.id), method: :delete, "data-confirm" => "本当に削除しますか？" %></td>
        </tboby>
    </table>
    <% end %>
    
    
    
    <h2>New Books</h2>


    <%= form_with model: Book.new do |f| %>

      <ladel>Title</ladel>
      <%= f.text_field :title %>
      
      <ladel>Boby</ladel>
      <%= f.text_area :body %><br>
      
      <%= f.submit 'Create Books' %>
      
    <% end %>
  </body>
</html>